TransAJ <- function(object) {
	AddClasses( object, c("AJ", "TP", "cboot") );
} # TransAJ

TransPAJ <- function(object) {
	AddClasses( object, c("PAJ", "TP", "cboot") );
} # TransPAJ

TransKMW1 <- function(object) {
	AddClasses( object, c("KMW1", "TP", "cboot") );
} # TransKMW1

TransKMW2 <- function(object) {
	AddClasses( object, c("KMW2", "TP", "cboot") );
} # TransKMW2

TransKMPW1 <- function(object) {
	AddClasses( object, c("KMPW1", "TP", "cboot") );
} # TransKMPW1

TransKMPW2 <- function(object) {
	AddClasses( object, c("KMPW2", "TP", "cboot") );
} # TransKMPW2

TransIPCW1 <- function(object) {
	AddClasses( object, c("IPCW1", "TP", "cboot") );
} # TransIPCW1

TransIPCW2 <- function(object) {
	AddClasses( object, c("IPCW2", "TPC", "rboot") );
} # TransIPCW2

TransLIN1 <- function(object) {
	AddClasses( object, c("LIN1", "TP", "cboot") );
} # TransLIN1

TransLIN2 <- function(object) {
	AddClasses( object, c("LIN2", "TPC", "rboot") );
} # TransLIN2

TransLS <- function(object) {
	AddClasses( object, c("LS", "TP", "cboot") );
} # TransLS
